<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Default styles -->
    <style name="MatchParent">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <style name="WrapContent">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="VerticalWrapping">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <!-- Button style -->
    <style name="AppButton">
        <item name="android:background">@drawable/ic_button</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <!-- StartActivity -->
    <style name="StartActivityButton" parent="AppButton">
        <item name="android:layout_width">@dimen/start_activity_button_width</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginBottom">@dimen/start_activity_button_margin_bottom</item>
    </style>

    <!-- QuestActivity -->
    <style name="SandwichButton">
        <item name="android:layout_width">@dimen/sandwich_size</item>
        <item name="android:layout_height">@dimen/sandwich_size</item>
        <item name="android:background">@drawable/ic_sandwich</item>
    </style>

    <style name="QuestButton" parent="AppButton">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

</resources>
